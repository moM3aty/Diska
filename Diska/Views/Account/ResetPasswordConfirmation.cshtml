@using System.Globalization
@{
    bool isAr = CultureInfo.CurrentCulture.Name.StartsWith("ar");
    ViewData["Title"] = isAr ? "تم التغيير بنجاح" : "Password Reset";
    Layout = null;

    string currentTheme = Context.Request.Cookies["theme"] ?? "light";
}

<!DOCTYPE html>
<html lang="@(isAr ? "ar" : "en")" dir="@(isAr ? "rtl" : "ltr")" data-bs-theme="@currentTheme">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - DISKA</title>

    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* =========================================
                   PREMIUM AUTH STYLES (Standalone)
                   ========================================= */
        :root {
            --auth-bg: #f8fafc;
            --auth-card-bg: #ffffff;
            --auth-text-main: #0f172a;
            --auth-text-muted: #64748b;
            --auth-border: #e2e8f0;
            --auth-primary: #0F172A;
            --auth-accent: #FACC15;
            --auth-shadow: 0 20px 40px -10px rgba(0,0,0,0.1);
            --success-color: #10b981;
            --success-bg: rgba(16, 185, 129, 0.1);
        }

        [data-bs-theme="dark"] {
            --auth-bg: #020617;
            --auth-card-bg: #1e293b;
            --auth-text-main: #f8fafc;
            --auth-text-muted: #94a3b8;
            --auth-border: rgba(255,255,255,0.05);
            --auth-primary: #6366f1;
            --auth-accent: #FACC15;
            --auth-shadow: 0 20px 40px -10px rgba(0,0,0,0.5);
            --success-color: #34d399;
            --success-bg: rgba(52, 211, 153, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Cairo', sans-serif;
            background-color: var(--auth-bg);
            color: var(--auth-text-main);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background-color 0.3s, color 0.3s;
        }

        .auth-container {
            width: 100%;
            max-width: 480px;
            padding: 20px;
        }

        .auth-card {
            background: var(--auth-card-bg);
            border: 1px solid var(--auth-border);
            border-radius: 24px;
            padding: 50px 40px;
            text-align: center;
            box-shadow: var(--auth-shadow);
            position: relative;
            overflow: hidden;
            transition: background-color 0.3s, border-color 0.3s;
        }

            /* Decorative Top Line */
            .auth-card::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 6px;
                background: linear-gradient(90deg, var(--success-color), var(--auth-accent));
            }

        .success-icon {
            width: 100px;
            height: 100px;
            background: var(--success-bg);
            color: var(--success-color);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 30px;
            font-size: 3rem;
            transition: 0.3s;
            box-shadow: 0 0 0 10px var(--auth-bg); /* Simulated ring */
        }

        .auth-title {
            font-size: 1.8rem;
            font-weight: 800;
            margin-bottom: 15px;
            color: var(--auth-text-main);
        }

        .auth-desc {
            color: var(--auth-text-muted);
            font-size: 1.1rem;
            margin-bottom: 40px;
            line-height: 1.6;
        }

        .btn-submit {
            width: 100%;
            padding: 16px;
            border-radius: 12px;
            background: var(--auth-primary);
            color: #fff;
            border: none;
            font-weight: 700;
            font-size: 1.1rem;
            cursor: pointer;
            transition: 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            text-decoration: none;
        }

            .btn-submit:hover {
                transform: translateY(-3px);
                box-shadow: 0 10px 25px -5px rgba(0,0,0,0.2);
                opacity: 0.95;
                color: #fff;
            }
    </style>
</head>
<body>

    <div class="auth-container">
        <div class="auth-card">

            <div class="success-icon">
                <i class="fas fa-check"></i>
            </div>

            <h1 class="auth-title">@(isAr ? "تم التغيير بنجاح" : "Password Reset")</h1>
            <p class="auth-desc">
                @(isAr ? "تم تحديث كلمة المرور بنجاح. يمكنك الدخول الآن إلى حسابك باستخدام كلمة المرور الجديدة." : "Your password has been reset successfully. You can now login with your new password.")
            </p>

            <a asp-action="Login" class="btn-submit">
                @(isAr ? "تسجيل الدخول" : "Login Now")
                <i class="fas fa-sign-in-alt"></i>
            </a>

        </div>
    </div>

</body>
</html>