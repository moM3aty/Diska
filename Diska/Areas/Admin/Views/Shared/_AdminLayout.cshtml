<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة التحكم - @ViewData["Title"]</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- استخدام نفس ملف الستايل للحفاظ على الهوية البصرية مع تعديلات بسيطة -->
    <link rel="stylesheet" href="~/style.css">
    <style>
        body {
            background-color: #f1f5f9;
        }

        .admin-wrapper {
            display: flex;
            min-height: 100vh;
        }

        .admin-sidebar {
            width: 250px;
            background: #0F172A;
            color: #fff;
            padding: 20px;
            position: fixed;
            height: 100%;
            right: 0;
        }

        .admin-content {
            flex: 1;
            margin-right: 250px;
            padding: 30px;
        }

        .admin-sidebar h3 {
            text-align: center;
            margin-bottom: 30px;
            color: #FACC15;
        }

        .admin-nav li {
            margin-bottom: 15px;
        }

        .admin-nav a {
            color: #cbd5e1;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 1.1rem;
            padding: 10px;
            border-radius: 8px;
            transition: 0.3s;
        }

            .admin-nav a:hover, .admin-nav a.active {
                background: #1e293b;
                color: #fff;
            }

        .stat-card {
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.05);
            text-align: center;
        }

            .stat-card h2 {
                font-size: 2.5rem;
                margin: 10px 0;
                color: #0F172A;
            }

            .stat-card p {
                color: #64748b;
                font-size: 0.9rem;
            }

        table {
            width: 100%;
            border-collapse: collapse;
            background: #fff;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        }

        th, td {
            padding: 15px;
            text-align: right;
            border-bottom: 1px solid #eee;
        }

        th {
            background: #f8fafc;
            color: #334155;
        }
        @@media (max-width: 768px) {
            .admin-sidebar

        {
            width: 60px;
            padding: 10px;
        }

        .admin-sidebar h3, .admin-nav span {
            display: none;
        }

        .admin-content {
            margin-right: 60px;
        }

        }
    </style>
</head>
<body>

    <div class="admin-wrapper">
        <!-- Sidebar -->
        <aside class="admin-sidebar">
            <h3>DISKA Admin</h3>
            <ul class="admin-nav">
                <li><a asp-area="Admin" asp-controller="Dashboard" asp-action="Index"><i class="fas fa-chart-line"></i> <span>الرئيسية</span></a></li>
                <li><a asp-area="Admin" asp-controller="Dashboard" asp-action="Products"><i class="fas fa-box"></i> <span>المنتجات</span></a></li>
                <li><a asp-area="Admin" asp-controller="Dashboard" asp-action="Orders"><i class="fas fa-shopping-cart"></i> <span>الطلبات</span></a></li>
                <li><a href="/"><i class="fas fa-home"></i> <span>زيارة الموقع</span></a></li>
                <li style="margin-top: 50px;"><a asp-controller="Account" asp-action="Logout" style="color: #ef4444;"><i class="fas fa-sign-out-alt"></i> <span>خروج</span></a></li>
            </ul>
        </aside>

        <!-- Main Content -->
        <main class="admin-content">
            @RenderBody()
        </main>
    </div>

</body>
</html>